version: '3.8'

services:
  monga-cal:
    build: .
    container_name: monga-cal
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./monga_cal.db:/app/monga_cal.db
      - ./config.yaml:/app/config.yaml
